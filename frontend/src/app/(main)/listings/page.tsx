export default function ListingsPage() {
  return (
    <main className="container mx-auto flex flex-1 flex-col px-4 py-16 text-center">
      <h1 className="text-3xl font-semibold">Browse listings</h1>
      <p className="mt-3 max-w-md text-sm text-muted-foreground">
        Search and filters will appear here once listings are wired to the API.
      </p>
    </main>
  );
}
